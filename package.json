{
  "name": "authforge-monorepo",
  "private": true,
  "workspaces": ["apps/dashboard", "packages/typescript-sdk"],
  "scripts": {"build": "npm run build --workspaces --if-present", "test": "npm run test --workspaces --if-present", "typecheck": "npm run typecheck --workspaces --if-present"}
}

